---
"@gasket/nextjs": patch
---

Document that a `GasketRequest` from the App Router has no `method`, and that
`getOriginalRequest` returns the assembled request-like rather than a framework
request.
