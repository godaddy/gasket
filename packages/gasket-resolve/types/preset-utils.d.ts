import type { Gasket } from '@gasket/engine';
import type { PresetInfo } from './loader';

export function flattenPresets(presetInfos: PresetInfo[]): PresetInfo[]

export function assignPresetConfig(gasket: Gasket): Gasket
